# 网站基本信息
NUXT_PUBLIC_SITE_URL=https://your-domain.com
NUXT_PUBLIC_SITE_NAME=Nuxt 模板项目
NUXT_PUBLIC_SITE_DESCRIPTION=一个集成了 UnoCSS、VueUse 和 SEO 优化的现代化 Nuxt 3 模板项目

# 国际化配置
NUXT_PUBLIC_DEFAULT_LOCALE=zh-CN

# 颜色模式配置
NUXT_PUBLIC_COLOR_MODE_PREFERENCE=system
NUXT_PUBLIC_COLOR_MODE_FALLBACK=light

# PWA 配置
NUXT_PUBLIC_PWA_SHORT_NAME=Nuxt App
NUXT_PUBLIC_PWA_THEME_COLOR=#3B82F6
NUXT_PUBLIC_PWA_BACKGROUND_COLOR=#ffffff

# 分析和监控（可选）
# NUXT_PUBLIC_GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX
# NUXT_PUBLIC_SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx

# API 配置
# 如果您的 API 部署在不同的域上，请取消注释并设置此项
# NUXT_PUBLIC_API_BASE=https://api.your-domain.com

# 如果您使用 Nuxt 内置的 Nitro 服务器作为 API，通常不需要设置此项，
# 因为默认值 '/api' 会指向服务器路由。但为了明确起见，可以设置如下：
# 在本地开发中，请使用完整的 URL，以确保 SSR 期间 API 请求能够正确解析。
# 如果您在不同的端口上运行前端和后端，请相应地修改此处的端口。
NUXT_PUBLIC_API_BASE=http://localhost:3000
# API_SECRET_KEY=your-secret-key

# 环境配置
NODE_ENV=development








